export function menu() {
    const element = document.createElement('div');

    element.innerHTML = "this menu div";

    document.body.appendChild(element);
}

export function title() {
    const element = document.createElement('div');

    element.innerHTML = "this title div";

    document.body.appendChild(element);
}
